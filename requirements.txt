# CPU-only base dependencies (torch, torchvision, numpy are pre-installed in Dockerfile)

# Basicsr & CodeFormer requirements
addict
future
yapf
gdown

# ML/CV libraries
facexlib
lpips
onnx
onnxruntime

# Web UI and image processing
streamlit
streamlit-image-comparison
opencv-python-headless
